#include<stdio.h>
#include<string.h>
int main(){
    char str[]="college wallah is best ";
    //char arr[]={'h','e','l','l','o','\0'};

    // arr[0]='M';     // change the item
    // arr[1]=97;

    // int i=0;
    // while(str[i]!='\0'){    // null charecter
    //     printf("%c",str[i]);    // i[str] dileo hobe
    //     i++;
    // }

    //printf("%s",str);
    puts(str);
    return 0;
}